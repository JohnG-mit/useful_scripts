# Example env for traffic_report.py
# Monthly limit in GB (float)
MONTHLY_LIMIT_GB=100
# 'tx' (only outgoing) or 'total' (rx+tx)
MONTHLY_LIMIT_MODE=tx
# Optional: override machine name
MACHINE_NAME=my-server
# Optional: specify interface (e.g., eth0). If empty, script will try to auto-detect
INTERFACE=
# Optional: feishu config for notifications
FEISHU_WEBHOOK_URL=
FEISHU_SECRET=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
SMTP_SERVER=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_TO=
LOG_FILE=
LOG_MAX_BYTES=
LOG_BACKUP_COUNT=
